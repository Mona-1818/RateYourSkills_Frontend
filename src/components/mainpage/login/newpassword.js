import React from 'react';
//forget password redirection email

export default function NewPass () {
    return (
        <div>
            otp
            <input type="text" />
            newpassword
            <input type="password" />
            <button>change password</button>
        </div>
    );
}